@page "/countries/edit/{Id:int}"

<h3>Editar Pais</h3>

@if(country == null)
{
	<Loading/>
}
else
{
	<FormWithName @ref="countryForm" Model="country" TextLabel="Pais:" OnValidSubmit="EditAsync" ReturnAction="Return" />
}
